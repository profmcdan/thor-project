services:
  db:
    image: postgres:18
    container_name: wallet_db_postgres
    restart: always
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${MAIN_POSTGRES_PASSWORD}
    ports:
      - "5435:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:alpine
    container_name: wallet_redis
    restart: always
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  pgbouncer:
    image: edoburu/pgbouncer:latest
    container_name: wallet_pgbouncer
    restart: always
    volumes:
      - ./pgbouncer:/etc/pgbouncer:ro
    ports:
      - "6432:5432"

  db-migrate:
    build: ./python-api
    container_name: wallet_db_migrate
    command: python manage.py migrate --noinput
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy

  api:
    build: ./python-api
    restart: always
    command: web
    expose:
      - "8000"
    scale: 4
    env_file:
      - .env
    environment:
      - PGBOUNCER_HOST=pgbouncer
      - PGBOUNCER_PORT=5432
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      pgbouncer:
        condition: service_started
      db-migrate:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8000/docs/ || exit 1"]
      interval: 3s
      timeout: 3s
      retries: 5

  nginx:
    image: nginx:alpine
    restart: always
    ports:
      - "8005:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      api:
        condition: service_healthy

  celery:
    build: ./python-api
    restart: always
    command: celery
    env_file:
      - .env
    environment:
      - PGBOUNCER_HOST=pgbouncer
      - PGBOUNCER_PORT=5432
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      pgbouncer:
        condition: service_started
      db-migrate:
        condition: service_completed_successfully

  celery-beat:
    build: ./python-api
    restart: always
    command: celery-beat
    env_file:
      - .env
    environment:
      - PGBOUNCER_HOST=pgbouncer
      - PGBOUNCER_PORT=5432
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      pgbouncer:
        condition: service_started
      db-migrate:
        condition: service_completed_successfully

  dotnet-api:
    build:
      context: ./dotnet-api
      dockerfile: Dockerfile
    restart: always
    expose:
      - "8000"
    scale: 4
    env_file:
      - .env
    environment:
      - POSTGRES_HOST=pgbouncer
      - POSTGRES_PORT=5432
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      pgbouncer:
        condition: service_started
      db-migrate:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8000/docs || exit 1"]
      interval: 3s
      timeout: 3s
      retries: 5

  nginx-dotnet:
    image: nginx:alpine
    restart: always
    ports:
      - "8006:80"
    volumes:
      - ./nginx-dotnet.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      dotnet-api:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    restart: always
    ports:
      - "5173:5173"
    env_file:
      - .env
    # depends_on:
    #   - nginx

volumes:
  postgres_data:
